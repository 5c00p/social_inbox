На другой машине:


git clone https://github.com/5c00p/social_inbox.git
# или, если уже клонирован:
git checkout main && git pull
Все ветки синхронизированы:

origin/main = 9be07ee ✅
origin/feature/task17 = 9fd9540
origin/feature/task18 = 9be07ee
Что НЕ ушло в репо (untracked, не Task 17/18 артефакты):

.claude/settings.json — твои локальные настройки Claude Code
Doc/Go_Live_Checklist.docx — твой draft (отдельный от docs/Go_Live_Checklist.docx, который сгенерён скриптом)
Локальный .env — секреты, не для git
Чтобы продолжить на другой машине, понадобится:

Установить uv, Docker, Node.js (для DOCX-генератора, опционально)
cp .env.example .env и заполнить
uv sync для Python deps
(если нужно генерить DOCX) npm install --prefix scripts
